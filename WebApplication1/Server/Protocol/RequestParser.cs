using WebApplication1.Shared.Models;

namespace WebApplication1.Server.Protocol
{
    public class RequestParser
    {
        public Request Parse(string json)
        {
            // Convert JSON string → Request object
            // Validate request format
            return new Request();
        }
    }
}
